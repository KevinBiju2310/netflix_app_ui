import 'package:flutter/material.dart';

class FastlaughsPage extends StatelessWidget {
  const FastlaughsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('FastlaughsPage'),
    );
  }
}